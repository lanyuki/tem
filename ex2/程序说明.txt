程序说明

    随机数生成器初始化：使用 srand(time(0)) 初始化随机数生成器，这样每次运行程序时都会生成不同的随机数。
    初始温度：程序开始时生成一个在-10到40之间的初始温度。
    生成新温度：每秒生成一个新的温度值。新的温度变化范围在-2到2之间。
    温度范围限制：确保温度值保持在-10到40之间。
    输出：打印当前温度。
    循环：程序每秒生成并打印一次新的温度值。